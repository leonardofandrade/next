# ============================================================================
# CONFIGURAÇÕES OBRIGATÓRIAS DE SEGURANÇA
# ============================================================================
# ⚠️ IMPORTANTE: NUNCA use valores padrão ou senhas fracas em produção!
# 
# Para gerar uma SECRET_KEY segura, execute:
# python manage.py shell -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
#
# OU use:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

# Django secret key (OBRIGATÓRIO - gere uma chave única e segura)
SECRET_KEY = 'SUA_SECRET_KEY_AQUI_GERE_UMA_CHAVE_SEGURA_E_UNICA'

# Simple JWT SECRET_KEY (opcional, se usar JWT)
SECRET_KEY_JWT = 'SUA_SECRET_KEY_JWT_AQUI_GERE_UMA_CHAVE_SEGURA_E_UNICA'

# 0 = False (Produção) - 1 = True (Desenvolvimento)
# ⚠️ NUNCA use DEBUG=1 em produção!
DEBUG = 1

# 0 = False - 1 = True
SELENIUM_HEADLESS = 1

# ============================================================================
# CONFIGURAÇÕES DO BANCO DE DADOS (OBRIGATÓRIAS)
# ============================================================================
# ⚠️ IMPORTANTE: Configure credenciais seguras e únicas para o banco de dados
# ⚠️ NUNCA use senhas padrão como "next123", "root123", "password", etc.

# MySQL
DATABASE_ENGINE = 'django.db.backends.mysql'
DATABASE_NAME = "next_db"
DATABASE_USER = "next"
DATABASE_PASSWORD = "SENHA_SEGURA_DO_BANCO_AQUI"
# DATABASE_HOST: Em Docker Compose, use "db" para o serviço de banco
# Para conexão local, use "127.0.0.1"
DATABASE_HOST = "127.0.0.1"
DATABASE_PORT = "3306"

# ============================================================================
# CONFIGURAÇÕES DE REDE E SEGURANÇA
# ============================================================================

# ALLOWED_HOSTS: Lista de hosts permitidos (separados por vírgula)
# ⚠️ Em produção, configure apenas os domínios reais da aplicação
# Exemplo: 'example.com, www.example.com'
# Para servidor Ubuntu 172.17.20.170, use: '172.17.20.170,localhost,127.0.0.1'
ALLOWED_HOSTS = 'localhost,127.0.0.1'

# INTERNAL_IPS: IPs internos para debugging (separados por vírgula)
INTERNAL_IPS = '127.0.0.1,localhost'

# CSRF_TRUSTED_ORIGINS: Origens confiáveis para CSRF (separadas por vírgula)
# ⚠️ IMPORTANTE: Para Django 4+, inclua scheme + host (http e https)
# Exemplo: 'https://example.com, http://example.com, https://www.example.com'
# Para servidor Ubuntu 172.17.20.170, use: 'http://172.17.20.170,http://172.17.20.170:80'
CSRF_TRUSTED_ORIGINS = 'https://localhost,http://localhost'

# CORS_ALLOWED_ORIGINS: Origens permitidas para CORS (separadas por vírgula)
# Configure apenas se a aplicação tiver frontend separado
CORS_ALLOWED_ORIGINS = 'http://127.0.0.1:5500,'

# ============================================================================
# CONFIGURAÇÕES DE PAGINAÇÃO
# ============================================================================

# PAGINATE_BY: Número padrão de itens por página nas views de listagem
# Valor padrão: 25
PAGINATE_BY = 25